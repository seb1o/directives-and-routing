import { SuperBtnDirective } from './super-btn.directive';

describe('SuperBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new SuperBtnDirective();
    expect(directive).toBeTruthy();
  });
});
